export const DEFAULT_DOMAIN_FALLBACK = 'ysweb.biz.id';

export const DEFAULT_DOMAINS: string[] = [];

export const DEFAULT_DOMAIN = DEFAULT_DOMAIN_FALLBACK;

export const DEFAULT_EMAIL: string = '';

export const getDefaultEmailDomain = () => {
  return DEFAULT_DOMAIN;
};
